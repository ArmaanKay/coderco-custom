name: CoderCo Custom Action CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest


    steps:
    - name: checkout code
      uses: actions/checkout@v2

    - name: hello_world_custom_action
      uses: ArmaanKay/coderco-custom@main
      with:
        who-to-greet: 'CoderCo Team'
